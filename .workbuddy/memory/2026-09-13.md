# 2026-09-13 工作日志

## 排查 GitHub Actions 打包失败（poker_gto_android）

**用户反馈**：「github 的 actions 失败了」。

**仓库**：`https://github.com/lizanle521/GTO.git`（**私有**）。私有仓库无法匿名读 Actions
日志（GitHub API 返回 404），所以拿不到原始报错，改为源码级静态排查。

### 定位到的根因（高置信）

**`buildozer.spec` 缺少 `android.accept_sdk_license = True`。**

证据来自 buildozer 源码：

```python
# buildozer/targets/android.py :: _android_update_sdk()
auto_accept_license = self.buildozer.config.getboolean(
    'app', 'android.accept_sdk_license', fallback=False)   # ← 默认 False

if auto_accept_license:
    kwargs["return_child"] = True
else:
    kwargs['show_output'] = True
```

且 buildozer 默认 spec 模板里该行是注释状态：

```
# android.accept_sdk_license = False
```

默认 False 时 buildozer 会以交互方式询问是否接受 SDK 许可证；GitHub Actions 无终端可输入，
SDK 组件装不上，构建在下载 SDK 阶段就挂，典型报错：

```
Failed to install the following Android SDK packages as some licences have not been accepted
```

配置里已补上该行，并加了注释说明「不要删」。

### 我这边的第二个错误（自己造成的）

旧 workflow 里写的是：

```bash
buildozer -v android debug 2>&1 | tail -n 200
```

**管道会让退出码变成 `tail` 的（永远 0）**，所以即使 buildozer 失败，这一步仍显示绿色，
真实错误被吞掉，只在最后的「上传 APK」步骤报 `if-no-files-found: error`，误导性很强。
已本地实测复现：`bash -c "exit 7" | tail -n 5` 的整条管道退出码确实是 0。

正确写法（已采用）：

```bash
set +e
yes | buildozer -v android debug 2>&1 | tee build.log
STATUS=${PIPESTATUS[1]}     # [0]=yes [1]=buildozer [2]=tee
exit "$STATUS"
```

实测注意：`yes` 在中途会收到 SIGPIPE 退出（141），**所以这里不能用 `set -o pipefail`**，
否则构建成功时反而会误判为失败。已用本地脚本验证 `PIPESTATUS[1]` 在成功/失败两条路径取值都正确。

### 顺带核对过的配置（确认无需改动）

拉了 p4a 和 buildozer 的源码/默认模板核对：

| 项 | 结论 |
|---|---|
| `p4a.branch = v2024.01.21` | tag 存在（commit `957a3e5f8c27`），推荐 NDK 25b、target API 33，MIN_TARGET_API=30 |
| `android.ndk = 25b` | 与 p4a v2024.01.21 的 `RECOMMENDED_NDK_VERSION` 一致（该版本只支持 NDK 25） |
| `android.api = 33` | 与 `RECOMMENDED_TARGET_API` 一致，且 >= MIN_TARGET_API(30) |
| `android.archs = arm64-v8a, armeabi-v7a` | 带空格是 buildozer 官方默认写法，解析没问题 |
| `android.features = android.hardware.camera` | 与官方模板示例 `android.hardware.usb.host` 同格式 |
| `requirements = python3,pillow,numpy` | 用 AST 扫过全部模块，第三方依赖只有 PIL/numpy/kivy，kivy 由 p4a 注入 |
| 字体回退 URL | 实测返回真实 OTF（`OTTO` 魔数），链接有效 |
| `ubuntu-22.04` | 仍是 buildozer 最稳的一代；弃用期 2026-09-17 开始，brownout 2027-03-23，下线 2027-04-17 |

### 本轮改动

- `poker_gto_android/buildozer.spec`：加 `android.accept_sdk_license = True`；排除
  `preflight.py`、`*.sh`、`build.log` 进包。
- `.github/workflows/build-apk.yml`：重写。13 步；`PIPESTATUS` 精确取退出码；完整日志
  `tee build.log` 并**无论成败都上传 artifact**；失败时把错误行写入 `$GITHUB_STEP_SUMMARY`；
  `actions/setup-java@v4`(temurin 17) + `actions/setup-python@v5`(3.11，避开 3.12 删除
  distutils 的坑)；`timeout-minutes: 90`；`PYTHONUTF8=1`；字体魔术字节校验（用 `od` 而非
  `xxd`，后者属 vim-common 不保证存在）。
- `poker_gto_android/preflight.py`：新增 3 项检查——SDK 许可证、NDK 与 p4a 版本匹配、
  目标 API 下限。**已做反向测试**：删掉 `accept_sdk_license` 那行后体检确实报 FAIL。
- `poker_gto_android/打包指南.md`：新增 SDK 许可证排错条目与「云端失败怎么查」流程。
- 新增根目录 `.gitignore`：忽略 `__pycache__`、`.buildozer`、`bin/`、`*.apk`、`build.log` 等。
- `git rm -r --cached` 清掉误入库的 10 个 `.pyc`（磁盘文件保留）。

### 验证结果

```
模块自检        7 个全 OK
单元测试        2 套全通过（test_postflop.py / test_app.py）
语法编译        全部 .py 正确
.bat 编码       两个都是纯 ASCII + CRLF
workflow YAML   pyyaml 解析通过，13 步结构正确
preflight       13 通过 / 2 警告 / 1 失败（唯一失败＝Windows 不能打包，预期）
shell 逻辑      magic_of、PIPESTATUS[1] 本地实测正确
```

### 待办

- 需要用户确认构建日志，验证根因判断（私有仓库我读不到日志）。
- 若补上许可证后仍失败，下一步怀疑方向：p4a 编译 numpy/pillow 时 NDK 工具链报错
  （届时看 `build-log` artifact）。
